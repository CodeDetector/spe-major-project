// const {createLogger, format, transports} = require("winston");

// const logger = createLogger({
//   format: format.json(),
//   transports: [new transports.Console(),new transports.File({level: 'error', filename: 'error.log'}) , new transports.File({level: 'info', filename: 'info.log'})],
// });

// module.exports = logger;